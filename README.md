# Telegram Bot для генерации изображений

Telegram-бот для генерации изображений с системой кредитов и подписок, построенный на aiogram 3 и FastAPI.

## 🎯 Функционал

- **Генерация изображений** через API с поддержкой FSM
- **Система кредитов** (10 стартовых кредитов для новых пользователей)
- **Покупка кредитов** через ЮKassa (3 тарифных плана)
- **Webhook обработка** для callback от API генерации и платежей
- **PostgreSQL база данных** с SQLAlchemy async

# Запуск на railway

1) Заполняешь env + url для webhook дан
2) Postgress

В проекте → Add Plugin
Выбери PostgreSQL
Railway создаст БД и добавит переменные окружения:
DATABASE_URL=postgresql+asyncpg://...

# Запуск локально

## 🧪 Технологии

- Python 3.12
- aiogram 3
- FastAPI
- SQLAlchemy async
- PostgreSQL
- YooKassa
- aiohttp

## 🚀 Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd my-dolly-ai
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # для Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

Создать базу данных

docker run -d \
  --name my-dolly-postgres \
  -e POSTGRES_DB=my_dolly_ai \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  postgres:16

📌 Что это значит:
БД: my_dolly_ai
пользователь: postgres
пароль: postgres
порт: 5432

Прописываешь
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/my_dolly_ai

6. Инициализируйте базу данных:
```bash
python -c "from database.base import init_db; import asyncio; asyncio.run(init_db())"
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните `.env` файл своими данными:
   - `BOT_TOKEN` - токен Telegram бота (получить у @BotFather)
   - `BOT_USERNAME` - username бота (без @)
   - `DATABASE_URL` - URL базы данных PostgreSQL
   - `GEN_API_KEY` - API ключ для генерации изображений
   - `CALLBACK_URL` - URL для callback от API генерации (для локальной разработки используйте ngrok)
   - `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` - данные для платежей
   - `YOOKASSA_WEBHOOK_URL` - URL для webhook от ЮKassa

## 🔧 Запуск

### Локальная разработка

Для локальной разработки используйте ngrok для получения публичного URL:

```bash (не нужен в railway - там url будет дан)
ngrok http 8000
```

Обновите `.env` файл с полученными URL:
- `CALLBACK_URL=https://your-ngrok-url.ngrok.io/gen/callback`
- `YOOKASSA_WEBHOOK_URL=https://your-ngrok-url.ngrok.io/yookassa/webhook`

Запустите приложение:
```bash
python main.py
```

Или используйте скрипт:
```bash
bash scripts/run_local.sh
```

### Production (Railway)

1. Подключите репозиторий к Railway
2. Добавьте все переменные окружения из `.env.example`
3. Railway автоматически запустит приложение

## 📋 Команды бота

- `/start` - Приветствие и регистрация (10 стартовых кредитов)
- `/photo` - Генерация изображения (стоимость: 2 кредита)
- `/buy_subscription` - Покупка кредитов
- `/status` - Проверка баланса кредитов

## 🏗 Структура проекта

```
.
├── main.py                 # Главный файл приложения
├── config/                 # Конфигурация
│   └── settings.py
├── handlers/               # Обработчики команд бота
│   ├── start.py
│   ├── photo.py
│   ├── subscription.py
│   └── status.py
├── states/                 # FSM состояния
│   └── image_generation.py
├── services/               # Сервисы
│   └── image_generation/
│       ├── client.py
│       └── tasks.py
├── payments/               # Платежи
│   └── yookassa_client.py
├── database/               # База данных
│   ├── base.py
│   ├── models.py
│   └── session.py
├── webhooks/               # Webhook обработчики
│   ├── gen_callback.py
│   └── yookassa.py
└── scripts/                # Скрипты
    └── run_local.sh
```

## 📝 Лицензия

MIT


